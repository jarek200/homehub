[project]
name = "homehub"
version = "0.1.0"
description = "HomeHub monorepo — SST uv workspace root"
requires-python = "==3.13.*"
dependencies = []

[tool.uv.workspace]
members = ["packages/rest-api"]
